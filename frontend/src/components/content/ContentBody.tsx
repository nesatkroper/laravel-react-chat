import React from "react";
import { ScrollArea } from "@/components/ui/scroll-area";

const ContentBody = () => {
  return (
    <React.Fragment>
      <ScrollArea className=" w-full h-[80vh] rounded-md border p-4"></ScrollArea>
    </React.Fragment>
  );
};

export default ContentBody;
